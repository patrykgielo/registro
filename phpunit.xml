<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         defaultTestSuite="Unit,Feature"
>
    <!--
        defaultTestSuite="Unit,Feature" (above) means `php artisan test` / plain
        `phpunit` with no testsuite flag ONLY runs Unit+Feature: verified against
        vendor/phpunit/phpunit/src/TextUI/Configuration/Merger.php around line 830,
        which falls back to phpunit's defaultTestSuite attribute exclusively when no
        testsuite CLI flag is given. The Browser suite below requires Chromium and
        Playwright plus a live in process HTTP server (see tests/Pest.php) and must
        never run implicitly in CI or a plain `php artisan test`. Run it explicitly
        by passing the testsuite flag "Browser" to `php artisan test`.
    -->
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Browser">
            <directory>tests/Browser</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="REDIS_CLIENT" value="phpredis"/>
        <env name="REDIS_HOST" value="127.0.0.1"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="APP_LOCALE" value="pl"/>
        <env name="APP_FAKER_LOCALE" value="pl_PL"/>
    </php>
</phpunit>
