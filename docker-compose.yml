services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Local development: Use opcache-dev.ini (validate_timestamps=On)
        # Production CI/CD: Will use default (production) for performance
        OPCACHE_MODE: dev
        # Enable browser testing with Playwright + Chromium
        # Set to "false" if you don't need E2E tests (saves ~800MB)
        BROWSER_TESTING: "true"
    container_name: registro-app
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - .:/var/www
    networks:
      - registro
    environment:
      - APP_URL=https://registro.local:8444
      - DB_HOST=mysql  # Use Docker service name
      - FILESYSTEM_DISK=public  # CRITICAL: Enables file uploads in Filament
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    container_name: registro-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-registro}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-password}
      MYSQL_PASSWORD: ${DB_PASSWORD:-password}
      MYSQL_USER: ${DB_USERNAME:-registro}
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    volumes:
      - ./docker/mysql:/docker-entrypoint-initdb.d
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - registro

  nginx:
    image: nginx:1.25-alpine
    container_name: registro-nginx
    restart: unless-stopped
    ports:
      - "8081:80"
      - "8444:443"
    volumes:
      - .:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./docker/ssl:/etc/nginx/ssl
    networks:
      - registro
    depends_on:
      - app

  node:
    build:
      context: ./docker/node
      dockerfile: Dockerfile
    container_name: registro-node
    working_dir: /var/www
    volumes:
      - .:/var/www
      - ./docker/ssl:/var/www/docker/ssl:ro  # Mount SSL certificates (read-only)
    ports:
      - "5173:5173"
    networks:
      - registro
    user: "1000:1000"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    depends_on:
      - app

  redis:
    image: redis:7.2-alpine
    container_name: registro-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - registro
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    command: redis-server --appendonly yes

  mailpit:
    image: axllent/mailpit:latest
    container_name: registro-mailpit
    restart: unless-stopped
    ports:
      - "8025:8025"  # Web UI
      - "1025:1025"  # SMTP
    networks:
      - registro
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1

  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: registro-queue
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    networks:
      - registro
    depends_on:
      - app
      - redis
      - mysql
    command: php artisan queue:work redis --sleep=3 --tries=3 --max-time=3600 --queue=emails,reminders,default

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: registro-scheduler
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    networks:
      - registro
    depends_on:
      - app
      - redis
      - mysql
    command: >
      sh -c "while true; do
        php artisan schedule:run --verbose --no-interaction
        sleep 60
      done"

  horizon:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: registro-horizon
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    networks:
      - registro
    depends_on:
      - app
      - redis
      - mysql
    command: php artisan horizon

networks:
  registro:
    driver: bridge

volumes:
  mysql_data:
    driver: local
  redis_data:
    driver: local